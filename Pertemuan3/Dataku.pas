
Program dataku;

Uses crt;

Var 
  npm_mahasiswa : string;
  nama_mahasiswa : string ;
  umur_mahasiswa : byte;
Begin
  clrscr;
  npm_mahasiswa := '222510097';
  nama_mahasiswa := 'Adhi F Hutagalung';
  umur_mahasiswa := 20;
  writeln(nama_mahasiswa);
  writeln(umur_mahasiswa);
  writeln(npm_mahasiswa);
  readln;
End.
